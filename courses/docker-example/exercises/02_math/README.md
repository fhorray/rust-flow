# Simple Math
This exercise demonstrates running a Python function.

## Task
Modify the `add` function to perform subtraction instead, and rename it to `subtract`?
(This is just a playground, the runner doesn't check for correctness yet).

## Goal
See the output in the console.
