# ☁️ Cloudflare Workers Setup Guide

To run the exercises in this course, you need to have **Bun** installed on your system.

## 🛠️ Installation Steps

### 1. Install Bun

Run the following in your terminal:

- **macOS / Linux / WSL**:
  ```bash
  curl -fsSL https://bun.sh/install | bash
  ```
- **Windows**: Bun is experimentally supported on Windows. Run:
  ```powershell
  powershell -c "irm bun.sh/install.ps1 | iex"
  ```

### 2. Verify Installation

After installing, open a **new** terminal and run:

```bash
bun --version
```

### 3. Check Again

Once you have verified the installation, click the **"Check Again"** button in Progy to continue.
