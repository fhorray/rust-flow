[workspace]
members = ["runner"]
exclude = ["content"]

[package]
name = "exercises"
version = "0.1.0"
edition = "2024"

# Dependencies for exercises (if needed in the future)
[dependencies]

[lib]
name = "exercises"
path = "src/lib.rs"
doctest = false
